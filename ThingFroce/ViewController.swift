//
//  ViewController.swift
//  ThingFroce
//
//  Created by Hari Kolasani on 2/21/16.
//  Copyright © 2016 BlueCloud Systems. All rights reserved.
//

import UIKit

class ViewController: UIViewController {

    override func viewDidLoad() {
        super.viewDidLoad()
        
        // Do any additional setup after loading the view, typically from a nib.
        
        //let appDelegate = UIApplication.sharedApplication().delegate as! AppDelegate
    }
    
    func sendRequest() {
        
        //let appDelegate = UIApplication.sharedApplication().delegate as! AppDelegate
        
        //            let req = appDelegate.oauth2!.request(forURL: <# resource URL #>)
        //            let task = appDelegate.oauth2!.session.dataTaskWithRequest(req) { data, response, error in
        //                if let error = error {
        //                    // something went wrong, check the error
        //                }
        //                else {
        //                    // check the response and the data
        //                    // you have just received data with an OAuth2-signed request!
        //                }
        //            }
        //            task.resume()
        
    }

    override func didReceiveMemoryWarning() {
        super.didReceiveMemoryWarning()
        // Dispose of any resources that can be recreated.
    }


}

